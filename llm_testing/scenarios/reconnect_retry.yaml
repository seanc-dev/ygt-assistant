name: reconnect_retry
mode: mock
steps:
  - action: http
    method: POST
    url: /actions/reconnect-v2
    json:
      simulate: expired
    expect:
      status: 200
  - action: grade
    rubric:
      must_include:
        - "Reconnect Microsoft"
        - "retry"
      disallow:
        - "token"
