name: triage_sla
mode: mock
steps:
  - action: http
    method: POST
    url: /actions/triage-v2
    json:
      filter: all
    expect:
      status: 200
  - action: grade
    rubric:
      must_include:
        - "SLA"
        - "<24h"
        - "<72h"
      prefer:
        - "priority"
