name: triage_sla
mode: mock
steps:
  - action: http
    method: POST
    url: /actions/triage-v2
    json:
      filter: all
    expect:
      status: 200
  - action: grade
    rubric:
      must_include:
        - "<24h"
        - "<72h"
        - "priority"
      prefer:
        - "rationale"
