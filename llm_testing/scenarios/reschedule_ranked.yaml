name: reschedule_ranked
mode: mock
steps:
  - action: http
    method: POST
    url: /actions/reschedule-v2
    json:
      event_id: mock-evt-1
    expect:
      status: 200
  - action: grade
    rubric:
      must_include:
        - "rank"
        - "rationale"
      disallow:
        - "booked"
