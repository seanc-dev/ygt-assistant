name: plan_today_buffers
mode: mock
steps:
  - action: http
    method: POST
    url: /actions/plan-today-v2
    json: {}
    expect:
      contains:
        - "plan"
        - "Buffer"
        - "Focus"
  - action: grade
    rubric:
      must_include:
        - "Buffer"
        - "Focus"
      disallow:
        - "booked" # no unsolicited bookings in mock
