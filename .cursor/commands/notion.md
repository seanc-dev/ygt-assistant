Please use the following instructions to update the tasks and projects databases where relevant.

Use Notion API via utils.notion_helper functions for all operations (queries and updates). For queries, use query_database(), get_items_by_status(), find_item_by_name(), or get_page_content(). For updates, use update_item_status(), update_page_property(), or create_task(). Database IDs: Tasks=29f795383d90808f874ef7a8e7857c01, Projects=29f795383d9080388644f9c25bdf2689. Status values: "Not started", "In progress", "Done". For text updates use {"rich_text": [{"text": {"content": "your text"}}]} format.

Instructions to follow.
